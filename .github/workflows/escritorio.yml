name: Compilar escritorio

# Alias: el flujo con los banners NSIS y los paquetes Linux está en
# build-installers.yml. Este archivo conserva el nombre antiguo para
# quien lo dispare a mano.

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  instalar:
    uses: ./.github/workflows/build-installers.yml
    secrets: inherit
